{% block dfn_oro_cronofy_auth_button_widget %}
    {#{% set options = {#}
        {#'url': path('')#}
    {#} %}#}
    <!--<div data-page-component-module=""
         data-page-component-options="{{ options|json_encode }}"> -->
        <div class="control-group">
            <div class="controls">
                <div style="margin-top: 10px;" class="ping-holder">
                    <button type="{{ type|default('button') }}" {{ block('button_attributes') }}>{{ 'dfn_oro_cronofy.form.auth_button'|trans({}, translation_domain) }}</button>
                    <div class="connection-status alert" style="display: none"></div>
                </div>
            </div>
        </div>
    <!-- </div> -->
{% endblock %}

{% block dfn_oro_cronofy_auth_button_row %}
    {{ block('button_row') }}
{% endblock %}
